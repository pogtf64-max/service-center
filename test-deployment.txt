# Тест автоматического развертывания

Этот файл создан для тестирования системы автоматического развертывания.

Время создания: $(Get-Date)
Статус: Готов к тестированию

## Инструкции по тестированию:

1. Внесите изменение в этот файл
2. Выполните: git add . && git commit -m "Test deployment" && git push origin main
3. Проверьте выполнение в GitHub Actions
4. Убедитесь что изменения применились на сервере

## Ожидаемый результат:

- GitHub Actions должен запуститься автоматически
- Скрипт деплоя должен выполниться на сервере
- Изменения должны примениться в Docker контейнере
- Приложение должно продолжать работать

## Проверка результата:

```bash
# Проверка логов GitHub Actions
# Перейдите в Actions → Deploy to Server → View logs

# Проверка сервера
ssh -i ~/.ssh/service_center root@77.110.127.57 "docker logs service-center-service-center-1 --tail 10"

# Проверка работоспособности
curl http://77.110.127.57
```

Если все работает правильно, система CI/CD настроена успешно! 🎉
